/**
 * JPA 엔티티 클래스들을 포함하는 패키지입니다.
 * 
 * <p>이 패키지는 다음과 같은 유형의 엔티티들을 포함합니다:</p>
 * 
 * <ul>
 *     <li>도메인 모델의 영속성을 위한 엔티티</li>
 *     <li>데이터베이스 테이블과 매핑되는 엔티티</li>
 *     <li>엔티티 간의 관계를 정의하는 엔티티</li>
 *     <li>데이터베이스 제약조건을 정의하는 엔티티</li>
 * </ul>
 * 
 * <p>특징:
 * <ul>
 *     <li>JPA 어노테이션을 사용하여 데이터베이스 테이블과 매핑됩니다.</li>
 *     <li>엔티티 간의 관계(일대다, 다대일, 다대다 등)를 정의합니다.</li>
 *     <li>기본적인 데이터베이스 제약조건을 정의합니다.</li>
 *     <li>도메인 모델과는 별개로 데이터베이스 스키마에 최적화됩니다.</li>
 * </ul>
 * </p>
 * 
 * <p>주의사항:
 * <ul>
 *     <li>엔티티는 도메인 로직을 포함하지 않습니다.</li>
 *     <li>엔티티는 가능한 한 단순하게 유지합니다.</li>
 *     <li>엔티티 간의 관계는 명확하게 정의합니다.</li>
 *     <li>성능을 고려한 적절한 인덱스를 설정합니다.</li>
 *     <li>엔티티의 변경은 데이터베이스 마이그레이션을 통해 관리합니다.</li>
 * </ul>
 * </p>
 */
package com.example.demo.infrastructure.persistence.entity; 