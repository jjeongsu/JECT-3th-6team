/**
 * Spring Data JPA 인터페이스를 정의하는 패키지입니다.
 * 
 * <p>이 패키지는 JpaRepository를 상속받는 Spring Data JPA 인터페이스들을 정의합니다.
 * 실제 DB 쿼리를 실행하는 역할을 하며, JPA 엔티티에 대한 기본 CRUD 작업을 제공하고
 * 커스텀 쿼리 메서드를 정의합니다.</p>
 * 
 * <p>주요 역할:</p>
 * <ul>
 *   <li>JpaRepository를 상속받는 Spring Data JPA 인터페이스</li>
 *   <li>실제 DB 쿼리를 실행하는 역할</li>
 *   <li>JPA 엔티티에 대한 기본 CRUD 작업 제공</li>
 *   <li>커스텀 쿼리 메서드 정의</li>
 * </ul>
 */
package com.example.demo.infrastructure.persistence.repository; 