/**
 * 도메인 모델 (Entity, Value Object)을 정의하는 패키지입니다.
 * 
 * <p>이 패키지는 비즈니스 도메인의 핵심 모델들을 정의합니다. 프레임워크나 외부 기술에 
 * 의존하지 않는 순수한 도메인 객체들로 구성되며, 비즈니스 규칙과 로직을 캡슐화합니다.</p>
 * 
 * <p>주요 역할:</p>
 * <ul>
 *   <li>비즈니스 도메인의 핵심 모델 정의</li>
 *   <li>프레임워크나 외부 기술에 의존하지 않는 순수한 도메인 객체</li>
 *   <li>비즈니스 규칙과 로직의 캡슐화</li>
 *   <li>도메인 지식의 표현</li>
 * </ul>
 */
package com.example.demo.domain.model; 