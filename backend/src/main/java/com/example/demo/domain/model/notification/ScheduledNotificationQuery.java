package com.example.demo.domain.model.notification;

import lombok.Getter;

/**
 * 스케줄된 알림 조회를 위한 쿼리 객체.
 */
@Getter
public class ScheduledNotificationQuery {
    // 필요하다면 필드 추가. 다른 도메인 모델과 일관성을 위해 우선 Query 클래스 생성함.
}
